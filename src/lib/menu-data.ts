import type { MenuItem } from '@/lib/types';

// We are now using Firestore (menu_items collection)
// This file is kept empty to prevent build errors in other components
export const menuItems: MenuItem[] = [];